spring:
  profiles:
    active:
    - dev
---
spring:
  profiles: dev
  application:
    name: microconfig-test-dev
mysql:
  url: aaa
---
spring:
  profiles: default
  application:
      name: microconfig-test-default
mysql:
  url: aaa
